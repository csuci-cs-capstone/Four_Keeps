
if(!place_snapped(global.tile_size,global.tile_size)){
	move_snap(global.tile_size, global.tile_size);
}

//TODO: are these things still needed?
image_speed = 0;
depth = -1

sprite_collision_mask(sprite_index, true, 1, 0, 0, 0, 0, 0, 0);








